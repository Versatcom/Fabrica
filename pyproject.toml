[project]
name = "fabrica"
version = "0.1.0"
description = "Import/export utilities for Fabrica."
requires-python = ">=3.11"
dependencies = [
    "openpyxl>=3.1.2",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["fabrica"]
