[project]
name = "fabrica-backend"
version = "0.1.0"
description = "Backend API for Fabrica PWA"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.111.0",
  "uvicorn>=0.30.0",
  "sqlmodel>=0.0.16",
]

[tool.uvicorn]
factory = false
